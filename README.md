# artforrichpeople.com

A storefront that sells one piece at a time.

## Run locally

It's an HTML file. Open it in a browser.

```bash
python3 -m http.server 8000
# → http://localhost:8000
```

## Deploy

Static site. Hosted on GitHub Pages, served from `main`.
